name: db-migrate-workflow

on:
  workflow_dispatch:
    inputs:
      environment:
        required: true
        type: string
      checkout_branch:
        required: true
        type: string
  push:
    branches:
      - patch/dbmigrate
    

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      checkout_branch: release/1.0
      DOCKER_IMAGE_NAME: db-migrate-image
      DOCKER_CONTEXT_FOLDER: python
      DOCKER_FILE: python/prohibition_web_svc/Dockerfile-migration
      DOCKER_IMAGE_TAG: latest
      namespace: dev
    steps:
      - name: Checkout
        uses: actions/checkout@v3
        with:
        #   ref: ${{ inputs.checkout_branch }}
          ref: ${{ env.checkout_branch }}

      - name: Setup
        uses: docker/setup-buildx-action@v2
        with:
          install: true

      - name: Login
        uses: docker/login-action@v2
        with:
          registry: ${{ secrets.DOCKER_REGISTRY }}
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Tag
        id: tag
        uses: mathieudutour/github-tag-action@v6.1
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          tag_prefix: ${{ env.DOCKER_IMAGE_NAME }}=v
          fetch_all_tags: true

      - name: Build
        uses: docker/build-push-action@v3
        with:
          context: ${{ env.DOCKER_CONTEXT_FOLDER }}
          file: ${{ env.DOCKER_FILE }}
          push: true
          tags: ${{ secrets.DOCKER_REGISTRY }}/dbe7-images/${{ env.DOCKER_IMAGE_NAME }}-${{ env.namespace }}:${{ env.DOCKER_IMAGE_TAG }}
      - name: Authenticate and set context for cluster ca
        uses: redhat-actions/oc-login@v1.2
        with:
          openshift_server_url: ${{ secrets.OPENSHIFT_SERVER_URL_SILVER }}
          openshift_token: ${{ secrets.OPENSHIFT_SA_PIPELINE_TOKEN_SILVER_ENV }}
          namespace: "${{ secrets.OPENSHIFT_LICENSE_PLATE_SILVER }}-${{ env.namespace }}"
      - name: Run Migration
        run: |
          oc process -f migrate-pod.yml --param IMAGE_NAME=${{ secrets.DOCKER_REGISTRY }}/dbe7-images/${{ env.DOCKER_IMAGE_NAME }}-${{ env.namespace }}:${{ env.DOCKER_IMAGE_TAG }} --param DB_HOST=${{ secrets.DB_HOST }} --param DB_NAME=${{ secrets.DB_NAME }}  --param DB_PORT=${{ secrets.DB_PORT }}  --param DB_USER=${{ secrets.DB_USER }}   --param DB_PASS=${{ secrets.DB_PASS }} | oc apply -f -